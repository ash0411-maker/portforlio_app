<!------ チャット件数が０だった場合 ------>
<% if @rooms.count == 0 %>
	<div class = "container">
		<div class = "row">
			<div class = "col-xs-8 col-xs-offset-1">
				<h2>
					チャットはまだありません。<br/>
					予約が入ればメッセージを送れます！
				</h2>
			</div>
		</div>
		<%= image_tag 'chat_example.jpeg', size: '1200x650' %>
	</div>

<!------ チャットをしていた場合 ------>
<% else %>
	<%= render "partial/room_index", rooms: @rooms %>
<% end %>


